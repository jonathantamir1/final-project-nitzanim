apiVersion: v1
kind: Secret
metadata:
  name: status-page-secrets
  namespace: status-page
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  SECRET_KEY: "your-secret-key-here"  # Base64 encoded
  DATABASE_PASSWORD: "your-db-password-here"  # Base64 encoded
  REDIS_PASSWORD: "your-redis-password-here"  # Base64 encoded
  EMAIL_HOST_USER: "your-email@example.com"  # Base64 encoded
  EMAIL_HOST_PASSWORD: "your-email-password-here"  # Base64 encoded
